# <变更名>

> 变更号：{{CHANGE_ID}}　变更类型：{{CHANGE_TYPE}}　目录：`docs/changes/{{CHANGE_DIR}}/`
> 本文件由 `new-change.sh` 脚手架生成——变更目录命名与变更号（{YYYYMMDD}-{NN}）由脚本分配，禁止手工创建目录。
> 流程（AGENTS.md「开发工作流（硬性规则）」10 步）：分析 → 脚手架 → 设计 → SOLID 自评 → 独立评审（中/大）→ 用户评审 → 实现 → 验证 → 一致性核对 → 蒸馏收尾。
> 小改动（1-3 文件、逻辑清晰）可省略本文件，把分析+取舍写进 commit message。
> **评审结论与实现偏差不写本文件**：用户评审 ✅/❌、实现偏差落在变更目录 `progress.md`；本文件只记设计与取舍。

## 背景与问题

（要解决的问题、触发场景、现状不足——一两段即可）

> 第 1 步先读：`docs/architecture.md`（现状）+ `docs/changes/README.md`（变更索引：曾做/曾否决 + 决策史入口）。

## 可选方案与取舍

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| A：... | | | 推荐 |
| B：... | | | 否决 |

（每案一句取舍理由；与曾被否决/被取代的相似方案的关系在结论注明）

## 方案

（选定的方案：核心思路、关键代码位置、与现有架构的衔接——现状以 `docs/architecture.md` 为准）

## 类职责与交互（中/大改动必填）

- 新类/改动类逐个列出：`类名` — 一句话职责（实现时写入类 javadoc）— 依赖方向（依赖谁/被谁依赖）
- 交互逻辑：类间调用链、数据流（谁产出、谁消费、状态归属）

> 第 4 步 SOLID 自评：按 SRP（职责单一）/ OCP（扩展不须改现有）/ LSP（继承不破坏替换）/ ISP（接口内聚）/
> DIP（依赖可逆）逐类 check；不满足回本节约设计。影响面须写具体符号（类名/文件/生成器），可 grep 验证。
> 第 5 步独立评审（中/大）：**新上下文实例**只带本文件 + `docs/architecture.md` + `docs/changes/README.md`（决策经索引/变更目录查阅），
> 输出「与项目现状/历史冲突清单」（含查索引：曾做/曾否决）——冲突清单回写下方「对账与冲突清单」节，不通过回本节约设计。

## 对账与冲突清单（可选）

（第 5 步独立评审输出的冲突清单与处置：逐条 `冲突 → 处置`（已纳入设计 / 已调整 / 否决理由））

## 改动文件与影响面

- 改动：`path/to/file.java`（改什么）
- 影响：依赖此逻辑的模块/生成器/契约（如 @Generated 所有权、config 键、SPI 签名）；引用相关变更用变更号 slug（如 `20260905-01-chore-project-memory`）
- 文档影响：是否改记忆文档（AGENTS.md / SKILL.md / architecture.md / changes/README.md / glossary）——是则收尾（10e）需跑「记忆文档自检用例集」

## 验证

- 测试：新增/修改哪些测试（含 PIT 关注点，如适用）
- 验证命令：`JAVA_HOME=... mvn clean test`（第 8 步）；第 9 步一致性核对将对照本文件「改动文件与影响面」「类职责与交互」核查实现偏差

## 评审与收尾（关卡；结论不写本文件）

- **第 6 步 用户评审**：通过 = 变更目录 `progress.md` 状态 ✅ + 索引行「完成」；否决/作废 = ❌ + 理由（仓库即审计记录）
- **第 9 步 一致性核对**：对照本文件「改动文件与影响面」「类职责与交互」核查实现偏差 → 记 `progress.md`「实现偏差」
- **第 10 步 蒸馏收尾**：决策与偏差 → 本变更目录 `progress.md`（**无顶层决策台账**；决策史 = changes/README 索引 + 各变更目录 + 初始归档 `20260801-01-feat-project-foundation`）；现状 → `docs/architecture.md`（更新条目，非散文追加）；索引行 → `docs/changes/README.md`；目录处置（完成保留；作废默认删除 + 索引行留痕；保留陈列个案改名加 `-rejected`/`-superseded` 后缀——规则见 changes/README「生命周期」）；改动记忆文档 → 跑自检用例集；`new-change.sh check`
