# <变更名>

> 变更号：{{CHANGE_ID}}　变更类型：{{CHANGE_TYPE}}　目录：`docs/changes/{{CHANGE_DIR}}/`
> 本文件只记设计与取舍；评审结论与实现偏差不写这里（落变更目录 `progress.md`）。

## 背景与问题

（要解决的问题、触发场景、现状不足——一两段即可）

## 可选方案与取舍

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| A：... | | | 推荐 |
| B：... | | | 否决 |

（每案一句取舍理由；与曾被否决/被取代的相似方案的关系在结论注明）

## 方案

（选定的方案：核心思路、关键代码位置、与现有架构的衔接——现状以 `docs/architecture.md` 为准）

## 类职责与交互（中/大改动必填）

- 新类/改动类逐个列出：`类名` — 一句话职责（实现时写入类 javadoc）— 依赖方向（依赖谁/被谁依赖）
- 交互逻辑：类间调用链、数据流（谁产出、谁消费、状态归属）

## 对账与冲突清单（可选）

（独立评审输出的冲突清单与处置：逐条 `冲突 → 处置`（已纳入设计 / 已调整 / 否决理由））

## 改动文件与影响面

- 改动：`path/to/file.java`（改什么）
- 影响：依赖此逻辑的模块/生成器/契约（如 @Generated 所有权、config 键、SPI 签名，写具体符号可 grep 验证）；引用相关变更用变更号 slug（如 `20260905-01-chore-project-memory`）
- 文档影响：是否改记忆文档（AGENTS.md / SKILL.md / architecture.md / changes/README.md / glossary）——是则收尾需跑「记忆文档自检用例集」

## 验收标准（完成 = 下列行为全部成立；中/大必填）

用"给定 … 应 …"写**用户可观察的结果**，不用"系统正常工作"这类不可验证的话。示例（DDL 生成器语境）：

> 1. 给定含 `@ignore` 列的 DDL → 生成的 Pojo/Mapper/XML 中均无该列，输出仅含 warning
> 2. 同一 DDL 连续执行两次 → 第二次所有文件无变化（幂等）
> 3. 目标文件含用户手写方法时再次生成 → 手写方法原样保留

每条的"应…"部分应是可写进测试断言的描述（测试由验收逐条派生）。

## 验证

- 测试：由「验收标准」逐条派生——落到哪些测试类/方法（含 PIT 关注点，如适用）
- 验证命令：`JAVA_HOME=... mvn clean test`
