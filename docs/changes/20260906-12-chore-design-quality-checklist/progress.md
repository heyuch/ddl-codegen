# 变更台账：20260906-12-chore-design-quality-checklist

## 状态

**实现完成**（2026-09-06：纯文档/流程；提交待落）

## 评审记录

- 2026-09-06 用户批准：① 落地「语义归层」检查项（07→11 教训）；② 设计自评引入 **GRASP 九原则**（信息专家/创建者/低耦合/高内聚/控制器/多态/纯虚构/间接/预防变化）；③ 设计模式使用前走取舍评估（能否用/哪种/简化了什么/复杂了什么/是否值得，不用模式也是结论）；④ 一并完善设计流程。chore（docs），design.md 记录方案与验收。

## 实现记录

- **SKILL.md**：步骤 4「SOLID 自评」→「设计自评（可回环）」，指向新增「设计自评检查单」小节（步骤 5 独立评审同此视角）：① SOLID；② GRASP 九项逐条一句话判据（含项目实证锚点：StatementApplier.applyOne 多态 20260830-03、static-rules-review §6 逻辑混杂 vs 元素繁多）；③ 语义归层（方言只存在于直接生产者/消费者一层，模型与共享服务只存真值；反例实证 20260906-07 → 20260906-11，slug 引用）；④ 设计模式取舍（五问 + 实证教训：不为简单开关建抽象——annotation-interceptors 作废）。
- **TEMPLATE.md**：「方案」补两句提示（模式取舍结论 / 语义归层）；「类职责与交互」补「按检查单 GRASP 说明职责归属判据」。提示均为指针式、不带子项编号（防 SKILL 重排漂移）。
- **glossary.md**：构建与流程表增 GRASP、设计模式两行（九项名称枚举 / 指针式出处，无判据复述）。
- **new-change.sh**：脚手架提示行「SOLID 自评」→「设计自评（设计自评检查单…）」（步骤 4 改名波及，随本变更同步）。

## 实现偏差（相对 design.md）

- 概念名统一：plan 中的「设计模式取舍四问」实为 ①–⑤ 五问，落地统一为**「设计模式取舍」**（不带计数，SKILL/glossary/TEMPLATE/脚本四处口径一致），消除名实不符。
- 检查单/模板条目不带子项编号（①②③），仅引节名——自检发现子项编号会随 SKILL 重排漂移。
- 自检（新上下文实例 10 问）修正 3 处：glossary「设计模式」行越界复述 → 收敛指针式；SKILL 反例实证裸号 `07/11` → slug；new-change.sh 残留「SOLID 自评」→ 同步。

## 边界（不做，随记录）

- 不改 AGENTS.md（「按其 10 步执行」仍成立）、changes/README 自检用例集（10 问不变）；语义归层通用词用「真值」（覆盖引号/格式等广义方言），architecture「存真名」指标识符语境，两处可并存。
- 小改动豁免规则不变（≤3 文件、逻辑清晰可省文档，设计自评仍应在脑中过一遍）。

## 收尾与自检

- 记忆文档自检（新上下文实例 10 问 + 复验）：合格——10 步结构与引用链成立、GRASP 判据/模式取舍单源（SKILL）、glossary/TEMPLATE 指针式、实证锚点真实存在；A1-A3 修复复验通过。
- `new-change.sh check` / `check-docs` 通过。

## 二次调整（2026-09-06 用户评审后继续本变更；追加提交）

- 用户提出 **战略/战术两层**设计决策框架（战略=项目整体架构匹配与融洽；战术=类/方法/字段的 OO 设计与模式取舍）；并指出初版检查单「③ 语义归层」是**过拟合**——方言/文本语义跨层流转是「职责不清」的**结果而非根因**，不应单列检查项。
- 调整：检查单改为**先战略后战术**两级——战略（架构融洽：归属层/架构不变量/复用优先/波及面/与历史决策，答案须指回 architecture 锚点或变更 slug）、战术（SOLID/GRASP/设计模式取舍）；删「语义归层」独立项，其判别启发与 07→11 反例并入「战略·归属层」；TEMPLATE 增「## 战略定位（必填）」节（归属层/不变量/波及面 2-3 行）；步骤 3/4/5 文案与新脚本提示同步两级措辞。
- 记忆自检复核（SKILL/TEMPLATE 再改动后 10 问）通过；`check-docs`/`check` 通过。

## 三次微调（2026-09-06 用户评审后继续本变更；追加提交）

- 用户指出：删除「语义归层」检查项后，战术区又补了一行自我指涉元注释（「…归因到职责归属即可，不单列检查项」）——**画蛇添足**：教训已完整落在战略「归属层」，该行是同义重复 + 解释自己为何不写的废话。已删除。
- 复盘：前次记忆自检未查出该问题——自检 10 问定位是**一致性回归**（文档↔文档↔代码/黑名单/跨文档单源），而该行是权威文档内部冗余，与一切「一致」故放行；且复核 prompt 只核对结构与互指、未要求评判文本是否必要。修正机制：自检用例集 卫生③ 扩展为同时检查**同一源头文档内部**的自我重复与自我指涉元注释（generic 表述，不绑定本次事故）。
- 扩展卫生③ 复检：删除目标干净、两级结构一致；判例边界维持——强调性 TL;DR（SKILL 硬性规则块）与机制说明（README/glossary 两处「不复述/防自指」注）豁免；顺带对齐记忆文档集合三处枚举（new-change.sh check-docs / changes-README 执行规则 / TEMPLATE 文档影响，现均含 static-rules-review.md 与 TEMPLATE.md）。

## 四次调整（2026-09-06 用户评审后继续本变更；追加提交）

- 用户指出自检慢的根因：历史变更目录每次被重复 review。**对象与边界**收窄——检查对象 = 记忆文档 + 本次改动的当前变更目录；历史变更目录自检已在各自收尾时完成、收尾后不可变，**不在检查对象内**，自检涉及历史时只作事实来源读取、不重新检查。历史类 2 问答案以索引行为准，不逐份展开历史目录。落于 changes/README「记忆文档自检用例集」。（注：该节初版 design 明写「不改自检用例集」，本调整经用户评审超出原边界——遵循本变更「评审驱动追加、记 progress」模式，不另立变更。）
- 记忆自检（按收窄范围执行：只喂记忆文档 + 当前变更目录，未开任何历史目录）通过；`check-docs`/`check` 通过。
