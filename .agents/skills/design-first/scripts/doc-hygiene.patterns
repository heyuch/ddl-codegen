# 记忆文档卫生黑名单（`new-change.sh check-docs` 使用）
# 每个模式 = 一次已清理的"无关内容"实证（自证/历史叙事/维护者注记）；新清理时把特征词追加在此（事故 → 永久回归用例）。
# 语义判不全的形态（如流程步号指针——SKILL 内部步号合法）靠「记忆文档自检用例集」卫生类问题抓，不用正则硬拦。
同源对症
曾因.*漂移
对账一遍
不赘述
格式器管不到
改规则只改本文件
无顶层决策台账
本项目同时是学习材料
观察期
历史注记：2026-08 早期
命名迁移对照
