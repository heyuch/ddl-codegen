# Checker Framework 与 Lombok 集成要求（见 checkerframework.org/manual/#lombok）：
# 生成成员带 @lombok.Generated，供检查器识别生成代码。
lombok.addLombokGeneratedAnnotation = true
# 生成成员自动带 @SuppressFBWarnings（spotbugs 跳过生成代码；需要 spotbugs-annotations 在编译 classpath）
lombok.extern.findbugs.addSuppressFBWarnings = true
